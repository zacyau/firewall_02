# 防火墙自动化运维平台 - 系统架构说明

## 一、项目概述

防火墙自动化运维平台是一个多品牌防火墙统一管理系统，支持华为、山石、新华三、瞻博四大厂商防火墙的策略自动化生成与下发。系统基于安全区域拓扑自动推导防火墙访问路径，并生成对应厂商的策略配置脚本。

**核心能力**：
- 设备配置管理（数据库驱动 + 配置文件种子）
- 安全区域拓扑建模与路径自动计算
- 多品牌策略脚本自动生成（Jinja2 模板）
- 地址组 / 端口组管理
- 策略冲突检测与冗余分析
- 集中式日志管理与异常追踪

---

## 二、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | FastAPI | 0.109.0 |
| 数据库 | SQLite (SQLAlchemy ORM) | 2.0.25 |
| 数据校验 | Pydantic | 2.5.3 |
| 模板引擎 | Jinja2 | 3.1.3 |
| SSH 连接 | Paramiko | 3.4.0 |
| ASGI 服务器 | Uvicorn | 0.27.0 |
| 前端框架 | Vue 3 | 3.4.15 |
| 前端路由 | Vue Router | 4.2.5 |
| HTTP 客户端 | Axios | 1.6.5 |
| CSS 框架 | Tailwind CSS | 3.4.19 |
| 构建工具 | Vite | 4.5.0 |

---

## 三、系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                          前端 (Vue 3 + Vite)                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 控制台   │ │ 设备管理 │ │ 策略生成 │ │ 组管理   │ │ 系统日志 │  │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │
│       └────────────┴────────────┴────────────┴────────────┘         │
│                           api.js (Axios)                            │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTP REST API
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      后端 (FastAPI + Uvicorn)                       │
│                                                                     │
│  ┌──────────────────── API 路由层 ─────────────────────┐           │
│  │  device.py  │ policy.py │ address_group.py │ port_group.py │ log.py │
│  └──────┬──────┴─────┬─────┴────────┬──────────┴──────┬─────┘           │
│         │            │             │                  │               │
│  ┌──────┴────────────┴─────────────┴──────────────────┴──────┐        │
│  │                    服务层 (Services)                       │        │
│  │  ConfigManager │ PolicyManager │ GroupManager │ PathCalculator │ PolicyValidator │
│  └──────┬────────────┬─────────────┬──────────────┬──────────┘        │
│         │            │             │              │                    │
│  ┌──────┴────────────┴─────────────┴──────────────┴──────────┐        │
│  │              基础设施层 (Infrastructure)                    │        │
│  │  Database (ORM) │ FirewallFactory │ Core (Logger+Exception) │ Jinja2 Templates │
│  └──────┬────────────┬─────────────┬──────────────┬──────────┘        │
│         │            │             │                                  │
│  ┌──────┴────────────┴─────────────┴──────────────┐                   │
│  │            适配器层 (Adapters)                   │                   │
│  │  HuaweiAdapter │ HillstoneAdapter │ H3CAdapter │ JuniperAdapter  │                   │
│  └────────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │  防火墙设备 (SSH)    │
                    │  华为 / 山石 /       │
                    │  新华三 / 瞻博      │
                    └─────────────────────┘
```

---

## 四、目录结构

```
firewall_02/
├── main.py                    # 应用入口，FastAPI 实例，路由注册，异常处理，启动初始化
├── requirements.txt           # Python 依赖
├── start.sh                   # 启动脚本
│
├── api/                       # API 层
│   ├── models.py              # Pydantic 请求模型定义
│   └── router/                # 模块化路由
│       ├── __init__.py        # 路由统一导出
│       ├── device.py          # 设备管理 API
│       ├── policy.py          # 策略管理 API
│       ├── address_group.py   # 地址组 API
│       ├── port_group.py      # 端口组 API
│       └── log.py             # 系统日志 API
│
├── services/                  # 业务服务层
│   ├── __init__.py            # 服务统一导出
│   ├── config_manager.py      # 设备配置管理器（数据库 CRUD + 种子导入）
│   ├── path_engine.py         # 防火墙路径计算引擎（核心算法）
│   ├── policy_manager.py      # 策略生成与管理
│   ├── group_manager.py       # 地址组/端口组管理
│   └── policy_validator.py     # 策略冲突检测与冗余分析
│
├── database/                  # 数据层
│   ├── __init__.py            # ORM 模型统一导出
│   └── models.py              # SQLAlchemy 数据模型 + Database 管理类
│
├── core/                      # 基础设施层
│   ├── __init__.py
│   ├── logger.py              # 集中式日志（RotatingFileHandler）
│   └── exception_handler.py   # 全局异常处理器
│
├── adapters/                  # 适配器层（多品牌防火墙适配）
│   ├── __init__.py
│   ├── base.py                # 适配器抽象基类
│   ├── huawei.py              # 华为防火墙适配器
│   ├── hillstone.py           # 山石防火墙适配器
│   ├── h3c.py                 # 新华三防火墙适配器
│   └── juniper.py             # 瞻博防火墙适配器
│
├── factory/                   # 工厂模式
│   ├── __init__.py
│   └── firewall_factory.py    # 防火墙适配器工厂
│
├── config/                    # 配置层
│   ├── __init__.py
│   └── devices.py             # 设备配置种子文件
│
├── templates/                 # Jinja2 策略模板
│   ├── huawei_policy.j2       # 华为策略模板
│   ├── hillstone_policy.j2    # 山石策略模板
│   ├── h3c_policy.j2          # 新华三策略模板
│   ├── juniper_policy.j2      # 瞻博策略模板
│   ├── address_group.j2       # 地址组配置模板
│   └── port_group.j2          # 端口组配置模板
│
├── logs/                      # 日志目录
│   └── .gitkeep
│
├── frontend/                  # 前端项目
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js          # Vite 配置（含 API 代理）
│   ├── tailwind.config.cjs     # Tailwind CSS 配置
│   ├── postcss.config.cjs
│   └── src/
│       ├── main.js             # Vue 应用入口
│       ├── App.vue             # 根组件
│       ├── assets/styles/main.css
│       ├── components/
│       │   └── AppLayout.vue    # 全局布局（侧边栏 + 顶栏 + 内容区）
│       ├── router/
│       │   └── index.js        # 前端路由定义
│       ├── services/
│       │   └── api.js          # API 请求封装（Axios）
│       └── views/
│           ├── Dashboard.vue          # 控制台
│           ├── DeviceList.vue         # 设备列表
│           ├── DeviceRegister.vue     # 设备注册/编辑
│           ├── PolicyGenerator.vue    # 策略生成
│           ├── PolicyList.vue         # 策略列表
│           ├── AddressGroupList.vue   # 地址组列表
│           ├── AddressGroupCreate.vue # 创建地址组
│           ├── PortGroupList.vue      # 端口组列表
│           ├── PortGroupCreate.vue    # 创建端口组
│           └── LogViewer.vue          # 系统日志浏览
│
├── scripts/                   # 脚本
│   └── test_api.py            # API 测试脚本
│
└── tests/                     # 单元测试
    └── test_policy_validator.py
```

---

## 五、后端架构详解

### 5.1 应用入口 (main.py)

**职责**：FastAPI 应用实例创建、中间件注册、路由挂载、全局异常处理、启动初始化。

**启动流程**：
1. 创建 FastAPI 实例，注册 CORS 中间件
2. 注册全局异常处理器（未捕获异常 / HTTP 异常 / 请求验证异常）
3. 挂载5个业务子路由（设备 / 策略 / 地址组 / 端口组 / 日志）
4. `startup` 事件触发：建表 → 数据库迁移 → 种子导入 → 日志记录

### 5.2 API 路由层 (api/router/)

按业务域拆分为5个独立路由模块，统一挂载到 `/api/v1` 前缀下：

| 路由模块 | 前缀 | 职责 |
|----------|------|------|
| device.py | `/api/v1` | 设备 CRUD、心跳检测、配置验证、种子导入、配置导出 |
| policy.py | `/api/v1` | 策略生成、策略应用、策略验证、策略查询 |
| address_group.py | `/api/v1` | 地址组 CRUD、配置生成、配置下发、状态查询 |
| port_group.py | `/api/v1` | 端口组 CRUD、配置生成、配置下发、状态查询 |
| log.py | `/api/v1` | 日志查询、日志清空、日志文件信息 |

**API 完整列表**：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/devices` | 获取所有设备 |
| POST | `/api/v1/devices` | 新增设备 |
| GET | `/api/v1/devices/{name}` | 获取单个设备 |
| PUT | `/api/v1/devices/{name}` | 更新设备 |
| DELETE | `/api/v1/devices/{name}` | 删除设备 |
| GET | `/api/v1/devices/{name}/heartbeat` | 心跳检测 |
| POST | `/api/v1/devices/validate` | 验证设备配置 |
| GET | `/api/v1/devices/export` | 导出设备配置 |
| POST | `/api/v1/devices/seed` | 重新导入种子 |
| POST | `/api/v1/policies/generate` | 生成策略 |
| POST | `/api/v1/policies/apply` | 应用策略 |
| POST | `/api/v1/policies/validate` | 策略验证 |
| GET | `/api/v1/policies` | 获取所有策略 |
| GET | `/api/v1/policies/{id}` | 获取单个策略 |
| POST | `/api/v1/groups/address` | 创建地址组 |
| GET | `/api/v1/groups/address` | 获取所有地址组 |
| GET | `/api/v1/groups/address/{name}` | 获取地址组 |
| PUT | `/api/v1/groups/address/{name}` | 更新地址组 |
| DELETE | `/api/v1/groups/address/{name}` | 删除地址组 |
| POST | `/api/v1/groups/address/{name}/generate` | 生成地址组配置 |
| POST | `/api/v1/groups/address/{name}/apply/{device}` | 应用地址组到设备 |
| POST | `/api/v1/groups/address/{name}/apply-all` | 批量应用地址组 |
| GET | `/api/v1/groups/address/{name}/status` | 地址组配置状态 |
| POST | `/api/v1/groups/port` | 创建端口组 |
| GET | `/api/v1/groups/port` | 获取所有端口组 |
| GET | `/api/v1/groups/port/{name}` | 获取端口组 |
| PUT | `/api/v1/groups/port/{name}` | 更新端口组 |
| DELETE | `/api/v1/groups/port/{name}` | 删除端口组 |
| POST | `/api/v1/groups/port/{name}/generate` | 生成端口组配置 |
| POST | `/api/v1/groups/port/{name}/apply/{device}` | 应用端口组到设备 |
| POST | `/api/v1/groups/port/{name}/apply-all` | 批量应用端口组 |
| GET | `/api/v1/groups/port/{name}/status` | 端口组配置状态 |
| GET | `/api/v1/logs` | 获取系统日志 |
| DELETE | `/api/v1/logs` | 清空日志 |
| GET | `/api/v1/logs/info` | 日志文件信息 |

### 5.3 服务层 (services/)

#### 5.3.1 ConfigManager - 设备配置管理器

**数据存储策略**：数据库为主存储，`config/devices.py` 仅作为初始化种子。

- 启动时检测数据库是否为空，为空则从种子文件导入
- 运行时所有读写操作直接操作数据库
- 提供设备 CRUD、种子导入、配置导出、配置验证

#### 5.3.2 PathCalculator - 路径计算引擎（核心算法）

**职责**：根据源/目标网络地址，自动推导流量经过的防火墙路径及各墙的源/目安全区域。

**初始化时构建3张映射字典**：

1. **direct_network_map**（直连网段映射）
   - Key: `n{网段}` (如 `n172.25.1.0/24`)
   - Value: `{firewall, zone}`
   - 收集范围：edge 区域网段 + forward-in 的 net（排除 0.0.0.0）+ forward-out 的 net + mixed 的 net

2. **middle_forward_map**（中转防火墙映射）
   - Key: 防火墙名称
   - Value: `{区域名: [关联设备列表]}`
   - 筛选条件：forward-in 区域的 dev 列表非空，或 mixed 区域的 dev 列表非空

3. **internet_forward_map**（互联网防火墙映射）
   - Key: `n{允许网段}`
   - Value: `{firewall, in: {src_zone, dst_zone}, out: {src_zone, dst_zone}}`
   - 对 forward-out 的 allow 网段，查找同墙 forward-in 中包含该网段的 trust_zone

**路径计算流程** (`calculate_path`)：

```
输入: src_net, dst_net
  │
  ├─ Step 1: 在 direct_network_map 中查找源/目标防火墙和区域
  │
  ├─ Step 2: 初始化 npf_list = [源墙, 目标墙]
  │
  ├─ Step 3: 判断同墙/跨墙
  │   ├─ 同墙 → 合并为一条记录，过滤 src_zone==dst_zone
  │   └─ 跨墙 → 继续
  │
  ├─ Step 4: 补全缺失 Zone
  │   ├─ 源墙 dst_zone: _complete_dst_zone()
  │   └─ 目标墙 src_zone: _complete_src_zone()
  │
  ├─ Step 5: 查找中间防火墙 (_find_middle_firewalls)
  │
  └─ Step 6: 过滤 src_zone==dst_zone 的无效规则
```

**安全区域类型定义**：

| 类型 | 含义 | 配置格式 |
|------|------|----------|
| edge | 边缘直连区域 | `{子区域名: [网段列表]}` |
| forward-in | 入方向转发区域 | `{区域名: {net: [网段], dev: [关联设备]}}` |
| forward-out | 出方向转发区域 | `{区域名: {net: [网段], allow: [允许网段]}}` |
| mixed | 混合区域 | `{区域名: {net: [网段], dev: [关联设备]}}` |

**CIDR 掩码支持**：使用 Python `ipaddress` 模块进行网段包含判断，支持 `/8`、`/16`、`/24` 等任意掩码。

#### 5.3.3 PolicyManager - 策略管理器

**职责**：基于路径计算结果，生成各防火墙的策略配置脚本。

**核心流程**：
1. 调用 PathCalculator 计算路径
2. 遍历路径上的每台防火墙
3. 通过 FirewallFactory 创建对应厂商适配器
4. 调用适配器的 `create_security_policy()` 生成脚本
5. 使用 Jinja2 模板渲染最终配置

**支持两种策略生成模式**：
- 单 IP 模式：直接指定 source_ip / dest_ip
- 地址组模式：指定 source_group / dest_group / port_group

#### 5.3.4 GroupManager - 组管理器

**职责**：地址组和端口组的 CRUD、配置脚本生成、批量下发。

**核心功能**：
- 地址组/端口组的创建、更新、删除、查询
- 基于模板生成各厂商的组配置脚本
- 组配置应用到指定设备或批量应用
- 查询组在各设备上的配置状态

#### 5.3.5 PolicyValidator - 策略验证器

**职责**：策略冲突检测与冗余分析。

**检测维度**：
- IP 范围重叠（使用 `ipaddress` 模块进行子网包含判断）
- 端口范围重叠
- 协议匹配
- 动作一致性

**重叠类型**：
| 类型 | 含义 |
|------|------|
| exact | 匹配条件完全重叠 |
| new_subset | 新规则匹配范围是已有规则的子集 |
| existing_subset | 已有规则匹配范围是新规则的子集 |
| partial | 部分重叠 |
| none | 无重叠 |

### 5.4 数据层 (database/)

**ORM 模型**（SQLAlchemy）：

| 模型 | 表名 | 说明 |
|------|------|------|
| FirewallDevice | firewall_devices | 防火墙设备（含 zones JSON 字段） |
| SecurityPolicy | security_policies | 安全策略 |
| PolicyAuditLog | policy_audit_logs | 策略审计日志 |
| AddressGroup | address_groups | 地址组 |
| PortGroup | port_groups | 端口组 |
| AddressGroupDeviceStatus | address_group_device_status | 地址组设备配置状态 |
| PortGroupDeviceStatus | port_group_device_status | 端口组设备配置状态 |

**数据库文件**：`database/firewall_platform.db`（SQLite）

### 5.5 适配器层 (adapters/)

**设计模式**：工厂模式 + 适配器模式

```
FirewallFactory.create_firewall(device_config) → FirewallAdapter 子类实例
```

**适配器接口**（抽象基类 `FirewallAdapter`）：

| 方法 | 说明 |
|------|------|
| `connect()` | SSH 连接防火墙 |
| `disconnect()` | 断开连接 |
| `check_heartbeat()` | 心跳检测 |
| `create_security_policy(config)` | 生成策略配置脚本 |
| `apply_policy(script)` | 应用策略到设备 |
| `get_zone_by_ip(ip)` | 根据 IP 判断所属安全区域 |
| `generate_cli_command(config)` | 生成 CLI 命令 |

**厂商适配器**：
- `HuaweiFirewallAdapter` - 华为（USG 系列）
- `HillstoneFirewallAdapter` - 山石
- `H3CFirewallAdapter` - 新华三
- `JuniperFirewallAdapter` - 瞻博

### 5.6 基础设施层 (core/)

#### logger.py - 集中式日志

- 日志文件：`logs/app.log`
- 日志轮转：5MB/文件，最多5个备份
- 日志格式：`[时间] 级别 模块名 - 消息`
- 双输出：文件(DEBUG+) + 控制台(INFO+)

#### exception_handler.py - 全局异常处理

| 处理器 | 捕获类型 | 行为 |
|--------|----------|------|
| `global_exception_handler` | `Exception` | 记录 ERROR 日志，返回 500 + 异常详情 + 堆栈 |
| `http_exception_handler` | `StarletteHTTPException` | 记录 WARNING 日志，返回对应状态码 |
| `validation_exception_handler` | `RequestValidationError` | 记录 WARNING 日志，返回 422 + 校验错误 |

---

## 六、前端架构详解

### 6.1 技术架构

- **框架**：Vue 3 (Composition API + `<script setup>`)
- **路由**：Vue Router 4 (History 模式)
- **样式**：Tailwind CSS 3
- **HTTP**：Axios (统一封装到 `api.js`)
- **构建**：Vite 4

### 6.2 页面路由

| 路径 | 组件 | 说明 |
|------|------|------|
| `/` | Dashboard.vue | 控制台（设备/策略统计概览） |
| `/devices` | DeviceList.vue | 设备列表管理 |
| `/devices/register` | DeviceRegister.vue | 注册/编辑设备 |
| `/groups/address` | AddressGroupList.vue | 地址组列表 |
| `/groups/address/create` | AddressGroupCreate.vue | 创建地址组 |
| `/groups/port` | PortGroupList.vue | 端口组列表 |
| `/groups/port/create` | PortGroupCreate.vue | 创建端口组 |
| `/policies/generate` | PolicyGenerator.vue | 策略生成（含路径可视化） |
| `/policies` | PolicyList.vue | 策略列表 |
| `/logs` | LogViewer.vue | 系统日志浏览 |

### 6.3 API 服务封装 (api.js)

统一 Axios 实例，baseURL 为 `/api/v1`，按业务域封装为5个 API 对象：

| 对象 | 方法 | 说明 |
|------|------|------|
| `deviceAPI` | getAll/get/create/update/delete/checkHeartbeat/validate/export/seed | 设备管理 |
| `policyAPI` | generate/generateDryRun/apply/getAll/get/validate | 策略管理 |
| `addressGroupAPI` | getAll/get/create/update/delete/generateConfigs/applyConfig/applyAll/getStatus | 地址组 |
| `portGroupAPI` | getAll/get/create/update/delete/generateConfigs/applyConfig/applyAll/getStatus | 端口组 |
| `logAPI` | getLogs/clearLogs/getInfo | 系统日志 |

### 6.4 全局布局 (AppLayout.vue)

- 左侧固定侧边栏（可折叠），包含6个菜单项
- 顶部标题栏，显示当前页面名称
- 右侧内容区，通过 `<router-view>` 渲染页面

### 6.5 Vite 代理配置

开发模式下，前端 (3000端口) 通过 Vite 代理将 `/api` 请求转发到后端 (8080端口)。

---

## 七、核心业务流程

### 7.1 策略生成流程

```
用户输入源/目标
      │
      ▼
PolicyGenerator.vue ──POST /policies/generate──▶ policy.py
      │                                              │
      │                                    PolicyManager.generate_policy()
      │                                              │
      │                                    ┌─────────┴──────────┐
      │                                    │ 解析地址组/端口组    │
      │                                    │ (从数据库查询)       │
      │                                    └─────────┬──────────┘
      │                                              │
      │                                    PathCalculator.calculate_path()
      │                                              │
      │                                    ┌─────────┴──────────┐
      │                                    │ 1. 查 direct_map   │
      │                                    │ 2. 判断同墙/跨墙   │
      │                                    │ 3. 补全 Zone       │
      │                                    │ 4. 查中间墙        │
      │                                    │ 5. 过滤无效规则     │
      │                                    └─────────┬──────────┘
      │                                              │
      │                                    遍历路径上每台防火墙
      │                                              │
      │                                    FirewallFactory.create_firewall()
      │                                              │
      │                                    adapter.create_security_policy()
      │                                              │
      │                                    Jinja2 模板渲染
      │                                              │
      ◀─────────── 返回策略脚本 + 路径信息 ──────────┘
```

### 7.2 设备管理流程

```
前端操作 ──▶ deviceAPI ──▶ device.py ──▶ ConfigManager
                                              │
                                    ┌─────────┴──────────┐
                                    │ 数据库 CRUD 操作     │
                                    │ (SQLAlchemy ORM)    │
                                    └─────────┬──────────┘
                                              │
                                    种子导入: config/devices.py → 数据库
```

### 7.3 日志管理流程

```
异常/错误发生
      │
      ▼
core/logger.py ──写入──▶ logs/app.log
                              │
前端 LogViewer.vue ──GET /logs──▶ log.py ──读取──▶ 返回日志内容
```

---

## 八、数据模型关系

```
┌──────────────────┐     ┌──────────────────────┐
│  FirewallDevice  │     │    SecurityPolicy    │
├──────────────────┤     ├──────────────────────┤
│ id (PK)          │     │ id (PK)              │
│ name (UK)        │     │ policy_name          │
│ vendor           │     │ source_ip            │
│ ip               │     │ dest_ip              │
│ port             │     │ protocol             │
│ username         │     │ dest_port            │
│ password         │     │ action               │
│ location         │     │ source_zone          │
│ description      │     │ dest_zone            │
│ zones (JSON)     │     │ device_name          │
│ status           │     │ policy_script        │
│ last_heartbeat   │     │ status               │
│ created_at       │     │ error_message        │
│ updated_at       │     │ created_at           │
└──────────────────┘     │ updated_at           │
                         └──────────────────────┘

┌──────────────────┐     ┌────────────────────────────┐
│  AddressGroup    │     │ AddressGroupDeviceStatus   │
├──────────────────┤     ├────────────────────────────┤
│ id (PK)          │     │ id (PK)                    │
│ name (UK)        │─────│ group_name                 │
│ description      │     │ device_name                │
│ addresses (JSON) │     │ status                     │
│ created_at       │     │ config_script              │
│ updated_at       │     │ applied_at                 │
└──────────────────┘     └────────────────────────────┘

┌──────────────────┐     ┌────────────────────────────┐
│  PortGroup       │     │ PortGroupDeviceStatus      │
├──────────────────┤     ├────────────────────────────┤
│ id (PK)          │     │ id (PK)                    │
│ name (UK)        │─────│ group_name                 │
│ description      │     │ device_name                │
│ ports (JSON)     │     │ status                     │
│ protocol         │     │ config_script              │
│ created_at       │     │ applied_at                 │
│ updated_at       │     └────────────────────────────┘
└──────────────────┘

┌──────────────────┐
│  PolicyAuditLog  │
├──────────────────┤
│ id (PK)          │
│ policy_id        │
│ action           │
│ operator         │
│ result           │
│ details          │
│ created_at       │
└──────────────────┘
```

---

## 九、设备配置格式

### 9.1 完整配置示例

```python
firewall_devices = {
    'USG6660': {
        'name': 'USG6660',
        'vendor': 'huawei',           # huawei / hillstone / h3c / juniper
        'ip': '172.31.255.2',
        'port': 22,
        'username': '',
        'password': '',
        'location': '',
        'description': '',
        'zones': {
            'edge': {                  # 边缘直连区域
                'Core_Server': ['172.25.1.0/24', '172.25.2.0/24'],
                'PC': ['172.27.1.0/24'],
            },
            'forward-in': {            # 入方向转发区域
                'untrust': {
                    'net': ['0.0.0.0/0'],   # 网段（0.0.0.0 表示默认区域）
                    'dev': [],              # 关联的远端防火墙
                },
            },
            'forward-out': {           # 出方向转发区域
                'untrust': {
                    'net': [],
                    'allow': ['172.28.32.0/24'],  # 允许通过的网段
                },
            },
            'mixed': {                 # 混合区域
                'dmz': {
                    'net': ['192.168.1.0/24'],
                    'dev': ['USG6615E'],
                },
            },
        },
    },
}
```

### 9.2 Zone 类型说明

| 类型 | 用途 | 配置格式 | 路径计算中的角色 |
|------|------|----------|------------------|
| edge | 直连网段区域 | `{子区域名: [CIDR网段]}` | 作为源/目标定位的依据 |
| forward-in | 入方向转发 | `{区域名: {net: [], dev: []}}` | 补全跨墙路径的 Zone |
| forward-out | 出方向转发 | `{区域名: {net: [], allow: []}}` | 互联网出口路径映射 |
| mixed | 混合区域 | `{区域名: {net: [], dev: []}}` | 中转墙区域关联 |

---

## 十、部署与运行

### 10.1 后端启动

```bash
# 安装依赖
pip install -r requirements.txt

# 启动服务（默认 8000 端口）
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 10.2 前端启动

```bash
cd frontend

# 安装依赖
npm install

# 开发模式（3000 端口，API 代理到 8080）
npm run dev

# 生产构建
npm run build
```

### 10.3 生产部署

前端构建产物 (`frontend/dist/`) 由后端静态文件服务或 Nginx 反向代理提供，API 请求通过反向代理转发到后端。

---

## 十一、设计模式与原则

| 模式/原则 | 应用位置 | 说明 |
|-----------|----------|------|
| 工厂模式 | `FirewallFactory` | 根据厂商类型创建对应适配器 |
| 适配器模式 | `adapters/` | 统一多品牌防火墙操作接口 |
| 单例模式 | `ConfigManager` | 通过 `get_config_manager()` 延迟初始化 |
| 模板方法模式 | `FirewallAdapter` 基类 | 定义统一接口，子类实现具体逻辑 |
| 分层架构 | 整体项目 | API层 → 服务层 → 数据层，职责分离 |
| 模块化路由 | `api/router/` | 按业务域拆分路由文件 |
| 种子模式 | `config/devices.py` | 配置文件仅作为数据库初始化种子 |

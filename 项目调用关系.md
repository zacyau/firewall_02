# 防火墙自动化运维平台 - 项目调用关系文档

## 一、项目目录结构

```
firewall_02/
├── main.py                      # 应用入口，FastAPI 启动（自动迁移+种子导入）
├── api/
│   ├── routes.py                # API 路由定义（所有 HTTP 接口）
│   └── models.py                # Pydantic 请求模型
├── services/
│   ├── path_engine.py           # 路径计算引擎（核心）
│   ├── policy_manager.py        # 策略管理器
│   ├── policy_validator.py      # 策略验证器
│   ├── group_manager.py         # 地址组/端口组管理器
│   └── config_manager.py        # 设备配置管理器（数据库 CRUD + 种子导入）
├── config/
│   ├── __init__.py
│   └── devices.py               # 防火墙设备配置文件（初始化种子）
├── database/
│   ├── __init__.py
│   └── models.py                # SQLAlchemy ORM 模型
├── adapters/
│   ├── base.py                  # 适配器基类（抽象接口）
│   ├── huawei.py                # 华为防火墙适配器
│   ├── hillstone.py             # 山石防火墙适配器
│   ├── h3c.py                   # 新华三防火墙适配器
│   └── juniper.py               # 瞻博防火墙适配器
├── factory/
│   └── firewall_factory.py      # 防火墙工厂类
├── templates/                   # Jinja2 策略模板
│   ├── huawei_policy.j2
│   ├── hillstone_policy.j2
│   ├── h3c_policy.j2
│   ├── juniper_policy.j2
│   ├── address_group.j2
│   └── port_group.j2
├── frontend/
│   └── src/
│       ├── router/index.js      # 前端路由
│       ├── services/
│       │   └── api.js           # 前端 API 客户端（统一）
│       └── views/               # Vue 页面组件
│           ├── Dashboard.vue
│           ├── DeviceList.vue
│           ├── DeviceRegister.vue
│           ├── PolicyGenerator.vue
│           ├── PolicyList.vue
│           ├── AddressGroupList.vue
│           ├── AddressGroupCreate.vue
│           ├── PortGroupList.vue
│           └── PortGroupCreate.vue
└── database/                    # SQLite 数据库文件目录
```

---

## 二、数据存储架构

项目采用**数据库为主、配置文件为种子**的存储架构：

| 存储方式 | 文件/位置 | 存储内容 | 读写方 | 说明 |
|---------|----------|---------|-------|------|
| **SQLite 数据库** | `database/firewall_platform.db` | 设备信息（含 zones）、地址组、端口组、安全策略、策略审计日志、组配置状态 | `ConfigManager`/`GroupManager`/`PolicyManager`/`PolicyValidator` 读写 | 主存储 |
| **配置文件** | `config/devices.py` | 防火墙设备信息（名称、厂商、IP、区域拓扑） | `ConfigManager.seed_from_config()` 读取 | 初始化种子，仅数据库为空时导入 |

### 数据库表结构

| 表名 | 用途 |
|-----|------|
| `firewall_devices` | 设备信息（含 zones 区域配置、description 描述） |
| `security_policies` | 生成的安全策略记录 |
| `policy_audit_logs` | 策略操作审计日志 |
| `address_groups` | 地址组定义 |
| `port_groups` | 端口组定义 |
| `address_group_device_status` | 地址组在各防火墙上的配置状态 |
| `port_group_device_status` | 端口组在各防火墙上的配置状态 |

### 配置文件数据结构 (`firewall_devices`)

```python
firewall_devices = {
    'USG6660': {
        'name': 'USG6660',
        'vendor': 'huawei',
        'ip': '172.31.255.2',
        'zones': {
            'edge': {                          # 末端区域（直连网段）
                'Core_Server': ['172.25.1.0/24', '172.25.2.0/24'],
                'PC': ['172.27.1.0/24'],
            },
            'forward-in': {                    # 入方向转发区域
                'untrust': { 'net': ['0.0.0.0/0'], 'dev': [] },
            },
            'forward-out': { ... },            # 出方向转发区域
            'mixed': { ... },                  # 混合区域
        },
    },
}
```

---

## 三、后端调用关系

### 3.1 应用启动流程

```
main.py::startup_event()
  ├── Database().create_tables()          # 初始化数据库表
  ├── _migrate_add_action_column(db)      # 数据库迁移：添加 action 列
  ├── _migrate_add_zones_column(db)       # 数据库迁移：添加 zones/description 列
  ├── config_manager.seed_from_config()   # 从配置文件种子导入（仅数据库为空时执行）
  └── config_manager.get_devices()        # 加载设备配置，打印设备数量
```

### 3.2 API 路由 → 服务层调用关系

#### 设备管理 API（统一路径 `/devices/`）

```
GET    /api/v1/devices                   → config_manager.get_devices()           # 获取所有设备
POST   /api/v1/devices                   → config_manager.add_device()            # 新增设备
POST   /api/v1/devices/validate          → config_manager.validate_device_config() # 验证配置
GET    /api/v1/devices/export            → config_manager.export_to_dict()        # 导出配置
POST   /api/v1/devices/seed              → config_manager.seed_from_config()      # 重新导入种子
GET    /api/v1/devices/{name}            → config_manager.get_device()            # 获取单个设备
PUT    /api/v1/devices/{name}            → config_manager.update_device()         # 更新设备
DELETE /api/v1/devices/{name}            → config_manager.delete_device()         # 删除设备
GET    /api/v1/devices/{name}/heartbeat  → FirewallFactory.create_firewall() → adapter.check_heartbeat()
```

#### 策略生成 API（核心流程）

```
POST /api/v1/policies/generate
  │
  ├── policy_manager.generate_policy(policy_config)
  │   │
  │   ├── [有地址组] → _get_address_groups_from_db()      # 从数据库读取地址组
  │   │                _get_port_groups_from_db()          # 从数据库读取端口组
  │   │                generate_policy_with_groups()       # 基于组的策略生成
  │   │                  │
  │   │                  ├── path_calculator.expand_ip_ranges(source_ips)     # 展开IP列表
  │   │                  ├── path_calculator.calculate_paths_for_ip_pairs()   # 批量路径计算
  │   │                  │     └── calculate_path(src_ip, dst_ip)             # 单条路径计算
  │   │                  │           ├── _find_network_in_direct_map()        # 查找直连网络
  │   │                  │           ├── _find_middle_firewalls()             # 查找中间防火墙
  │   │                  │           ├── _complete_src_zone()                 # 补全源区域
  │   │                  │           └── _complete_dst_zone()                 # 补全目的区域
  │   │                  │
  │   │                  ├── path_calculator.merge_paths_by_consistency()     # 合并相同路径
  │   │                  │
  │   │                  └── [对每条路径的每个防火墙]
  │   │                        ├── path_calculator.get_policy_config()        # 获取策略配置
  │   │                        ├── FirewallFactory.create_firewall()          # 创建适配器
  │   │                        └── adapter.create_security_policy()           # 生成CLI脚本
  │   │                              └── _render_template("xxx_policy.j2")    # Jinja2模板渲染
  │   │
  │   └── [无地址组] → 直接调用 calculate_path() + 生成策略
  │
  └── [dry_run=true] → policy_validator.validate_generated_policies(result)
```

#### 策略应用 API

```
POST /api/v1/policies/apply
  │
  └── policy_manager.apply_policy(policy_config, simulate)
        │
        ├── [simulate=true]  → 保存策略到数据库，状态=applied
        │
        └── [simulate=false] → 连接真实设备下发
              ├── _get_device_by_name()              # 从数据库获取设备
              ├── FirewallFactory.create_firewall()   # 创建适配器
              ├── adapter.apply_policy(script)        # SSH下发配置
              └── 更新策略状态 + 记录审计日志
```

#### 策略验证 API

```
POST /api/v1/policies/validate
  │
  └── policy_validator.validate_rules(device_name, direction, rules)
        ├── _get_device(device_name)                  # 从数据库获取设备
        ├── _get_existing_rules(device_name)          # 从数据库获取已有策略
        ├── _check_conflicts()                        # 冲突检测
        └── _check_redundancy()                       # 冗余分析
```

#### 地址组/端口组 API

```
POST   /api/v1/groups/address              → group_manager.create_address_group()      # 写数据库
GET    /api/v1/groups/address              → group_manager.get_all_address_groups()    # 读数据库
PUT    /api/v1/groups/address/{name}       → group_manager.update_address_group()      # 写数据库
DELETE /api/v1/groups/address/{name}       → group_manager.delete_address_group()      # 写数据库
POST   /api/v1/groups/address/{name}/generate
  │
  └── group_manager.generate_address_group_configs()
        ├── _get_all_devices()                          # 从数据库获取设备列表
        └── [对每台设备] _generate_address_group_config()
              ├── FirewallFactory.create_firewall()     # 创建适配器
              └── adapter._render_template("address_group.j2")

POST   /api/v1/groups/address/{name}/apply/{device}  → group_manager.apply_address_group_config()
POST   /api/v1/groups/address/{name}/apply-all        → group_manager.apply_address_group_to_all()

# 端口组 API 结构同上，端口组模板为 port_group.j2
```

### 3.3 服务层内部依赖关系

```
┌──────────────────────────────────────────────────────────────────┐
│                        api/routes.py                             │
│                       (FastAPI Router)                            │
└──────┬──────────┬──────────────┬──────────────┬─────────────────┘
       │          │              │              │
       ▼          ▼              ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐
│Config    │ │Policy    │ │Group     │ │Policy        │
│Manager   │ │Manager   │ │Manager   │ │Validator     │
│(DB CRUD  │ │          │ │          │ │              │
│ +Seed)   │ │          │ │          │ │              │
└────┬─────┘ └──┬───────┘ └──┬───────┘ └──────┬───────┘
     │          │             │                 │
     │          ▼             │                 │
     │    ┌──────────┐       │                 │
     │    │Path      │       │                 │
     │    │Calculator│       │                 │
     │    └──────────┘       │                 │
     │                       │                 │
     ▼                       ▼                 ▼
┌──────────────────────────────────────────────────────┐
│              database/ (SQLite)                       │
│              FirewallDevice (含 zones)                │
│              + config/devices.py (种子)               │
└──────────────────────────────────────────────────────┘
     │                       │                 │
     │          ┌────────────┴─────────┐       │
     ▼          ▼                      ▼       ▼
┌──────────────────────┐  ┌──────────────────────────┐
│  factory/             │  │  database/               │
│  FirewallFactory      │  │  Database + ORM Models   │
│  └─> adapters/        │  │  (SQLite)                │
│      ├─ HuaweiAdapter │  └──────────────────────────┘
│      ├─ HillstoneAdpt │
│      ├─ H3CAdapter    │
│      └─ JuniperAdapter│
│  └─> templates/*.j2   │
└──────────────────────┘
```

### 3.4 路径计算引擎核心流程

```
PathCalculator.__init__(devices_config)
  ├── _build_direct_network_map()     # 构建 {网段 → {防火墙, 区域}} 映射
  │     遍历所有设备的 edge 区域，提取直连网段
  │
  ├── _build_middle_forward_map()     # 构建 {防火墙 → {对端防火墙: [中间防火墙]}} 映射
  │     遍历所有设备的 forward-in/forward-out/mixed 区域
  │     根据 dev 列表推导防火墙间转发关系
  │
  └── _build_internet_forward_map()   # 构建 {防火墙 → {默认出接口区域}} 映射
        遍历 forward-out/mixed 区域，提取默认出口

PathCalculator.calculate_path(src_ip, dst_ip)
  │
  ├── Step 1: _find_network_in_direct_map(src_ip)  → src_fw, src_zone
  ├── Step 2: _find_network_in_direct_map(dst_ip)  → dst_fw, dst_zone
  │
  ├── Step 3: [src_fw == dst_fw]  → 同一防火墙，直接返回
  │
  ├── Step 4: _find_middle_firewalls(src_fw, dst_fw)  → 中间防火墙列表
  │     ├── 查 middle_forward_map
  │     └── 递归查找路径（深度限制10跳）
  │
  ├── Step 5: _complete_src_zone()  → 补全源防火墙的出方向区域
  ├── Step 6: _complete_dst_zone()  → 补全目的防火墙的入方向区域
  │
  └── Step 7: 组装完整路径 [{firewall, src_zone, dst_zone}, ...]
```

---

## 四、前端调用关系

### 4.1 前端路由 → 页面组件

```
/                          → Dashboard.vue          # 仪表盘
/devices                   → DeviceList.vue         # 设备列表
/devices/register          → DeviceRegister.vue     # 添加/编辑设备
/policies/generate         → PolicyGenerator.vue    # 策略生成
/policies                  → PolicyList.vue         # 策略列表
/groups/address            → AddressGroupList.vue   # 地址组列表
/groups/address/create     → AddressGroupCreate.vue # 创建地址组
/groups/port               → PortGroupList.vue      # 端口组列表
/groups/port/create        → PortGroupCreate.vue    # 创建端口组
```

### 4.2 页面组件 → API 调用

| 页面组件 | 使用的 API 模块 | 调用的接口 |
|---------|---------------|-----------|
| `Dashboard.vue` | `deviceAPI`, `policyAPI` | `GET /devices`, `GET /policies` |
| `DeviceList.vue` | `deviceAPI` | `GET /devices`, `DELETE /devices/{name}`, `GET /devices/{name}/heartbeat` |
| `DeviceRegister.vue` | `deviceAPI` | `GET /devices/{name}`, `POST /devices`, `PUT /devices/{name}` |
| `PolicyGenerator.vue` | `policyAPI`, `addressGroupAPI`, `portGroupAPI` | `POST /policies/generate`, `POST /policies/generate?dry_run=true`, `POST /policies/apply`, `GET /groups/address`, `GET /groups/port` |
| `PolicyList.vue` | `policyAPI` | `GET /policies` |
| `AddressGroupList.vue` | `addressGroupAPI` | `GET /groups/address`, `DELETE /groups/address/{name}`, `POST /groups/address/{name}/generate`, `POST /groups/address/{name}/apply-all` |
| `AddressGroupCreate.vue` | `addressGroupAPI` | `POST /groups/address`, `PUT /groups/address/{name}` |
| `PortGroupList.vue` | `portGroupAPI` | `GET /groups/port`, `DELETE /groups/port/{name}`, `POST /groups/port/{name}/generate`, `POST /groups/port/{name}/apply-all` |
| `PortGroupCreate.vue` | `portGroupAPI` | `POST /groups/port`, `PUT /groups/port/{name}` |

### 4.3 前端 API 客户端

```
frontend/src/services/api.js
  ├── deviceAPI       → /api/v1/devices/*           # 设备管理（统一，含 CRUD + heartbeat + validate + export + seed）
  ├── policyAPI       → /api/v1/policies/*
  ├── addressGroupAPI → /api/v1/groups/address/*
  ├── portGroupAPI    → /api/v1/groups/port/*
  └── healthAPI       → /health
```

#### deviceAPI 方法列表

| 方法 | HTTP | 端点 | 功能 |
|------|------|------|------|
| `getAll()` | GET | `/devices` | 获取所有设备 |
| `get(name)` | GET | `/devices/{name}` | 获取单个设备 |
| `create(data)` | POST | `/devices` | 新增设备 |
| `update(name, data)` | PUT | `/devices/{name}` | 更新设备 |
| `delete(name)` | DELETE | `/devices/{name}` | 删除设备 |
| `checkHeartbeat(name)` | GET | `/devices/{name}/heartbeat` | 检查心跳 |
| `validate(data)` | POST | `/devices/validate` | 验证配置 |
| `export()` | GET | `/devices/export` | 导出配置 |
| `seed()` | POST | `/devices/seed` | 重新导入种子 |

---

## 五、核心业务流程

### 5.1 策略生成完整流程（最核心）

```
用户操作                    前端                          后端
─────────                ──────                        ──────
选择源地址组 ──────→ PolicyGenerator.vue
选择目的地址组        │
选择端口组            │
点击"生成策略" ──────→ policyAPI.generate() ──────→ routes.generate_policy()
                                                            │
                                                            ▼
                                                 PolicyManager.generate_policy()
                                                            │
                                                 ┌──────────┴──────────┐
                                                 │ 读取地址组/端口组(DB) │
                                                 │ 展开IP列表            │
                                                 │ 计算IP对路径          │
                                                 └──────────┬──────────┘
                                                            │
                                                            ▼
                                                 PathCalculator.calculate_paths_for_ip_pairs()
                                                            │
                                                 ┌──────────┴──────────┐
                                                 │ 查找源/目的直连防火墙  │
                                                 │ 查找中间防火墙        │
                                                 │ 补全区域信息          │
                                                 │ 合并相同路径          │
                                                 └──────────┬──────────┘
                                                            │
                                                            ▼
                                                 [对路径上每个防火墙]
                                                 ├── get_policy_config()
                                                 ├── FirewallFactory.create_firewall()
                                                 └── adapter.create_security_policy()
                                                       └── _render_template()
                                                            │
                                                            ▼
                                                 返回 {firewall_policies, path_group_details}
                                                            │
                        ←──── 返回JSON结果 ←────────────────┘
                        │
  显示防火墙路径         │
  显示策略脚本           │
  点击"应用到防火墙" ──→ policyAPI.apply() ─────→ routes.apply_policy()
                                                            │
                                                 PolicyManager.apply_policy()
```

### 5.2 设备管理流程

```
用户操作                    前端                          后端
─────────                ──────                        ──────
访问设备列表 ──────→ DeviceList.vue
                    │
                    ├── deviceAPI.getAll() ──────→ routes.get_all_devices()
                    │                                  └── config_manager.get_devices()  # 从数据库
                    │
  点击"添加设备" ──→ DeviceRegister.vue
                    │
                    ├── deviceAPI.create(data) ──→ routes.add_device()
                    │                                  └── config_manager.add_device()   # 写入数据库
                    │
  点击"编辑" ──────→ DeviceRegister.vue?name=xxx
                    │
                    ├── deviceAPI.get(name) ────→ routes.get_device()
                    │                                  └── config_manager.get_device()    # 从数据库
                    │
                    ├── deviceAPI.update(name,data) → routes.update_device()
                    │                                  └── config_manager.update_device() # 更新数据库
                    │
  点击"删除" ──────→ deviceAPI.delete(name) ──→ routes.delete_device()
                                                       └── config_manager.delete_device() # 删除数据库记录
```

### 5.3 应用启动与数据初始化流程

```
uvicorn main:app
  │
  ├── startup_event()
  │     ├── Database().create_tables()              # 创建所有数据库表
  │     ├── _migrate_add_action_column(db)           # 迁移：security_policies 添加 action 列
  │     ├── _migrate_add_zones_column(db)            # 迁移：firewall_devices 添加 zones/description 列
  │     ├── config_manager.seed_from_config()        # 种子导入（仅数据库为空时执行）
  │     │     ├── 读取 config/devices.py
  │     │     ├── 检查数据库是否已有设备
  │     │     └── [数据库为空] → 逐条写入 FirewallDevice 表
  │     └── config_manager.get_devices()             # 打印已加载设备数量
  │
  └── FastAPI 就绪，等待 HTTP 请求
```
